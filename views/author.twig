{% extends "layout.twig" %}
{% block content %}
    <div class="author">
        {{ title }}
        {% for post in posts %}
            {# ADD LOOP CONTENT HERE #}
        {% endfor %}
    </div>
{% endblock %}
