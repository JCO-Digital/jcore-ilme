{# fields is the acf fields variable #}
{# and block is the block metadata like name and id #}
	{% if fields.show_all == 1 %}
		<section id="block-{{ block.id }}" class="block {{ block.className }} align{{ block.align}}"> {% if yoast is not empty %}
			{% include 'partials/footer-social.twig' %}
		{% endif %}
	</section>
{% endif %}
